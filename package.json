{
  "name": "qaddons",
  "version": "1.16.4",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "check": "node scripts/check.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
